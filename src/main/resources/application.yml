spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/user?currentSchema=user
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
